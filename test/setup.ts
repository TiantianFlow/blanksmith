// Vitest global setup. Domain-layer tests are pure and run under jsdom; this
// file is intentionally minimal so later tasks can extend it (e.g. chrome API
// mocks) without touching the domain test files.
